# EnvironmentFile for the pmax_exporter systemd unit.
# Supplies Unisphere secrets referenced as ${VAR} in config.yaml.
#
# Install:  /etc/pmax_exporter/pmax_exporter.env   (chmod 0600, chown root:pmax)

PMAX1_PASSWORD=change-me
# PMAX2_PASSWORD=change-me
