replicaCount: 1

image:
  repository: ghcr.io/fjacquet/pmax_exporter
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 9443

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

livenessProbe:
  httpGet:
    path: /health
    port: http
readinessProbe:
  httpGet:
    path: /health
    port: http

volumes: []
# - name: config-templated
#   emptyDir:
#     sizeLimit: 16Mi
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

volumeMounts: []
# - mountPath: /app/config
#   name: config-templated
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

extraArgs: []

existingSecret: ""

# Inline exporter config, rendered into a Secret and mounted at /etc/pmax_exporter/config.yaml.
# This is a free-form passthrough — add any config here, including the optional `otel:` block
# to enable OTLP push (off by default). Secrets should be injected via `env:` or a
# passwordFile reference rather than stored literally here.
config: |
  server:
    host: "0.0.0.0"
    port: "9443"
    uri: "/metrics"
  collection:
    interval: "5m"
    timeout: "120s"
    maxConcurrent: 8
    volumeMetrics: false
    volumeInventory: false
    volumeStorageGroups: []
  otel:
    enabled: false
    endpoint: "localhost:4317"
    insecure: true
    interval: "30s"
  servers:
    - name: unisphere-prod-01
      host: "${PMAX1_HOSTNAME}"
      port: 8443
      username: "${PMAX1_USERNAME}"
      password: "${PMAX1_PASSWORD}"
      insecureSkipVerify: true
      apiVersion: "100"
      arrays: []

env: []
# - name: PMAX1_HOSTNAME
#   value: unisphere.example.com
# - name: PMAX1_USERNAME
#   value: monitor
# - name: PMAX1_PASSWORD
#   valueFrom:
#     secretKeyRef:
#       name: my-pmax-secret
#       key: password

deploymentAnnotations: {}

podAnnotations:
  prometheus.io/scrape: "false"
  prometheus.io/path: "/metrics"
  prometheus.io/port: "9443"

prometheus:
  monitor:
    enabled: false
    additionalLabels: {}
    interval: 15s
    namespace: []
    metricRelabelings: []
    relabelings: []
    targetLabels: []

  rules:
    enabled: false
    additionalLabels: {}
    namespace: ""
    additionalRules: []

  scrapeConfig:
    enabled: false
    additionalLabels: {}
    namespace: ""
    interval: 60s
    scrapeTimeout: 55s
    metricRelabelings: []
    relabelings: []
