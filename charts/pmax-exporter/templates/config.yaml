{{- if and (not .Values.existingSecret) .Values.config }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pmax-exporter.fullname" . }}-config
  labels:
    {{- include "pmax-exporter.labels" . | nindent 4 }}
stringData:
  config.yaml: {{ tpl .Values.config . | quote }}
{{- end }}
