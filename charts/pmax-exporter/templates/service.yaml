apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmax-exporter.fullname" . }}
  labels:
    {{- include "pmax-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pmax-exporter.selectorLabels" . | nindent 4 }}
